// An example Backbone application contributed by  [Jérôme Gravel-Niquet](http://jgn.me/). This demo uses a simple [LocalStorage adapter](backbone-localstorage.html) to persist Backbone models within your browser.
var app = app || [];

var ENTER_KEY = 13;

// creating the **App**.
$(function(){
	new app.AppView();
});
